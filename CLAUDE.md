# 네이버 마스터 (Naver Master)

> **전자동 네이버 카페·블로그 운영 관리 및 결과보고 시스템**
> 이 파일은 프로젝트의 헌장이자 영구 기억이다. 모든 세션은 작업 시작 전 이 파일을 기준으로 삼는다.

## 미션

총사령관(사용자)이 유튜브에 공구(공동구매) 소개 영상을 송출하면, **사용자의 관여가 거의 없이**:

1. 송출 **하루 뒤(D+1)** 그 영상을 AI로 파악한다 (내용 분석 + 장면 캡처).
2. 시선을 끄는 제목과 함께 내용을 정리해 **네이버 카페와 블로그에 포스팅**한다.
   - 포스팅에는 반드시 **① 해당 유튜브 영상 링크**와 **② 구매 페이지 링크**가 함께 들어간다.
3. 포스팅에 달린 **댓글을 확인하고 답글**을 단다.
4. **매일 아침 카톡**으로 보고한다: 어떤 댓글이 달렸고 어떻게 답했는지, 반응은 어떤지, 오늘 카페·블로그 방문자가 몇 명인지.
5. 사용자가 건별 승인을 할 수 없으므로, 사용자를 **대리해 승인하는 자동승인 시스템**이 게시 여부를 판정한다.

## 실행 환경

- **머신**: alfredo (MacBook Pro M5, macOS) — 모든 자동화는 이 머신에서 돈다.
- **스케줄러**: launchd (macOS 표준) + 파이썬 파이프라인.
- **데이터**: SQLite 단일 DB (`ops/state/naver_master.db`) + 파일 기반 작업 큐 (`ops/tasks/`).

## 지휘 체계

| 역할 | 누구 | 책임 |
|---|---|---|
| 총사령관 | 사용자 (ryoshin74) | 정책 결정, 아침 보고 수신, 비상정지. 그 외 관여하지 않는다. |
| 총괄 지휘관 | Claude (이 세션) | 전체 지시·디테일 설계·판단. 행동대장에게 작업지시서를 내리고 결과보고서를 받아 다음 지시를 만든다. |
| 행동대장 1~6 | 컴퓨터유즈 가능한 코덱스 AI 세션 6개 | 지시서를 받아 브라우저/GUI 실작업 수행 후 결과보고서 + 증빙(스크린샷) 제출. |

지휘관↔행동대장 사이의 지시·보고는 전부 **핸즈오프**(파일 큐 기반, 사람 개입 없음)로 오간다.
프로토콜 상세: `docs/ORCHESTRATION.md`

## 일일 파이프라인 (7단계)

```
P1 감지   유튜브 새 영상 감지 → D+1 작업 예약
P2 분석   영상·자막 다운로드 → 키 프레임 캡처 → 상품/가격/포인트 추출
P3 생성   카페용·블로그용 제목 후보 + 본문 + 캡처 배치 + 영상링크 + 구매링크
P4 검수   자동승인 시스템 판정 (PASS / HOLD / FAIL)  ← docs/APPROVAL_POLICY.md
P5 게시   PASS 건을 카페·블로그에 게시, 게시 결과 검증
P6 응대   새 댓글 수집 → 답글 생성 → 검수 → 게시 (주기 실행)
P7 보고   매일 아침 카톡으로 일일 보고 (댓글/답글/반응/방문자/보류 건)
```

## 불변 원칙

1. **사용자 본인 채널만 운영한다.** 본인 카페·본인 블로그·본인 영상·본인 상품. 타인 카페 무단 홍보, 댓글 스팸, 다계정 운영은 하지 않는다.
2. **사람 같은 페이싱.** 일일 게시 한도·게시 간 최소 간격·랜덤 지터를 지킨다. 네이버 계정 보호가 시스템 생존 조건이다.
3. **게시는 반드시 자동승인 게이트를 통과한 것만.** 게이트 우회 게시는 어떤 경우에도 금지.
4. **모든 게시·답글은 증빙(스크린샷+URL)과 함께 DB에 기록**한다. 아침 보고는 DB에서 생성한다.
5. **비상정지**: `ops/state/PAUSE` 파일이 존재하면 모든 게시·답글이 즉시 중단된다 (수집·보고는 계속).
6. 민감 댓글(클레임, 환불, 분쟁성)은 자동 답변하지 않고 HOLD 후 아침 보고에 올린다.

## 저장소 구조 (목표)

```
naver-master/
├── CLAUDE.md                  ← 이 파일 (헌장·영구 기억)
├── docs/
│   ├── PLAN.md                ← 종합 기획서 (아키텍처·기술선택·로드맵)
│   ├── ORCHESTRATION.md       ← 지휘관↔행동대장 핸즈오프 프로토콜
│   └── APPROVAL_POLICY.md     ← 자동승인(대리 승인) 시스템 정책
├── src/                       ← 파이프라인 코드 (Python)
├── ops/
│   ├── tasks/agent-{1..6}/    ← 행동대장별 inbox/doing/done 작업 큐
│   ├── state/                 ← SQLite DB, PAUSE 플래그, 토큰
│   └── reports/daily/         ← 일일 보고 사본
└── config/                    ← 채널 ID·카페 ID·한도 등 설정 (비밀키는 .env, 커밋 금지)
```

## 작업 규칙

- 개발 브랜치: `claude/great-planck-l3y8eb`. 커밋·푸시는 이 브랜치로.
- 문서는 한국어로 작성한다.
- API 키·쿠키·토큰은 절대 커밋하지 않는다 (`.env`, `ops/state/` 는 gitignore).
- 기획이 바뀌면 이 파일과 `docs/`를 먼저 고치고 코드를 고친다.
